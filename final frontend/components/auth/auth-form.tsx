"use client"

import Link from "next/link"
import { useRouter } from "next/navigation"
import { Box, LockKeyhole, Mail, UserRound } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs"
import { ThemeToggle } from "@/components/theme-toggle"

export function AuthForm({ mode }: { mode: "login" | "signup" }) {
  const router = useRouter()
  const signup = mode === "signup"
  return <main className="grid min-h-dvh lg:grid-cols-2"><section className="relative hidden overflow-hidden border-r bg-primary p-10 text-primary-foreground lg:flex lg:flex-col"><Link href="/" className="flex items-center gap-3 font-medium"><span className="flex size-9 items-center justify-center rounded-md bg-primary-foreground text-primary"><Box /></span>Fabrication Lab</Link><div className="my-auto max-w-lg"><p className="font-mono text-xs uppercase tracking-[0.2em] text-primary-foreground/60">Identity gateway / secure</p><h1 className="mt-5 text-balance text-6xl font-semibold tracking-tight">Build access starts here.</h1><p className="mt-6 text-lg leading-relaxed text-primary-foreground/70">One account for training, equipment scheduling, faculty review, maintenance, and your digital lab credential.</p></div><p className="font-mono text-xs text-primary-foreground/50">SYSTEM STATUS / NOMINAL</p></section><section className="flex min-h-dvh flex-col"><header className="flex h-16 items-center justify-between border-b px-6"><Link href="/" className="font-mono text-xs uppercase tracking-wider">Back to lab</Link><ThemeToggle /></header><div className="flex flex-1 items-center justify-center p-6"><Card className="w-full max-w-md border-0 shadow-none"><CardHeader><CardTitle className="text-3xl">{signup ? "Request lab access" : "Enter the lab portal"}</CardTitle><CardDescription>{signup ? "Create an institutional profile. Faculty accounts require approval." : "Use your institutional credentials to continue."}</CardDescription></CardHeader><CardContent><form id="auth-form" className="flex flex-col gap-5" onSubmit={(event) => { event.preventDefault(); router.push("/portal") }}>{signup && <><Tabs defaultValue="student"><TabsList className="grid w-full grid-cols-2"><TabsTrigger value="student">Student</TabsTrigger><TabsTrigger value="faculty">Faculty</TabsTrigger></TabsList></Tabs><div className="flex flex-col gap-2"><Label htmlFor="name">Full name</Label><div className="relative"><UserRound className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><Input id="name" className="pl-9" autoComplete="name" required /></div></div></>}<div className="flex flex-col gap-2"><Label htmlFor="email">Institutional email</Label><div className="relative"><Mail className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><Input id="email" type="email" className="pl-9" autoComplete="email" placeholder="name@university.edu" required /></div></div><div className="flex flex-col gap-2"><div className="flex justify-between"><Label htmlFor="password">Password</Label>{!signup && <button type="button" className="text-xs text-muted-foreground hover:text-foreground">Forgot password?</button>}</div><div className="relative"><LockKeyhole className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><Input id="password" type="password" className="pl-9" autoComplete={signup ? "new-password" : "current-password"} minLength={8} required /></div></div></form></CardContent><CardFooter className="flex-col gap-4"><Button type="submit" form="auth-form" className="w-full">{signup ? "Create access request" : "Sign in"}</Button><p className="text-center text-sm text-muted-foreground">{signup ? "Already registered?" : "Need an account?"} <Link href={signup ? "/login" : "/signup"} className="font-medium text-foreground underline underline-offset-4">{signup ? "Sign in" : "Request access"}</Link></p></CardFooter></Card></div></section></main>
}
